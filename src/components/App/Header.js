import React, { PropTypes } from 'react';

const Header = () => {
    const title = 'Search Synonyms';

    return (
        <h1>
            { title }
        </h1>
        );
};

export default Header;
