import React, { PropTypes } from 'react';

const Header = () => {
    const title = 'Search Cinnamons';

    return (
        <h1>
            { title }
        </h1>
        );
};

export default Header;
